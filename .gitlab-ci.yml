stages:
  - package
 
build_jar:
  stage: package
  tags:
    - ahmad
  script:
    - echo "Packaging Maven Project"
    - chmod +x app-version.sh
    - ./set-version.sh
    - mvn clean package
  artifacts:
    paths:
      - target/*.jar