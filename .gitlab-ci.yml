stages:
  - package
 

package:
  stage: package
  tags:
    - ahmad 
  script:
    - echo "Packaging Maven Project"
    - chmod +x app-version.sh
    - ./app-version.sh
    - rm -rf target
    - mvn clean package
  artifacts:
    paths:
      - target/*.jar